version: "3"

services:
  hermod:
    build:
      context: .
      dockerfile: dockerfile
    image: grokepeer/hermod:0.2.3
    command: ""
    ports:
      - 2088:2088
    environment:
      - DEL_TOKEN=testing
